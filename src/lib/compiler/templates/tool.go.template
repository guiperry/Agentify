package main

import (
	"fmt"
)

// {{.toolName}} implements the {{.toolDescription}}
func {{.toolName}}(params map[string]interface{}) (interface{}, error) {
	// Parse parameters
	{{.parameterParsing}}

	// Tool implementation
	{{.toolImplementation}}
}

// Tool registration is handled by the main plugin initialization